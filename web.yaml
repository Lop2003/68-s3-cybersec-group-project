services:
  s3web:
    image: caddy:latest
    container_name: 68-s3-web
    restart: unless-stopped
    ports:
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
    depends_on:
      - s3app
